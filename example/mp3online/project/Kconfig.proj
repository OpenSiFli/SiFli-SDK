#APP specific configuration.
config BF0_HCPU
    bool
    default y

config CUSTOM_MEM_MAP
    bool
    select custom_mem_map
    default y if !SOC_SIMULATOR

config ENABLE_DFU
    bool "enable dfu and add dfu sub-project"
    default n

config WEBCLIENT_DEBUG
    bool "enable webclient debug"
    default n

# The image cache size is shared by all examples now, see
# middleware/lvgl/Kconfig_mem. Give the value this project needs here:
#
#   CONFIG_IMAGE_CACHE_IN_PSRAM_SIZE=1000000
rsource "../src/Kconfig"