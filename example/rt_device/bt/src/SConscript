import os
from building import *

cwd = GetCurrentDir()

# Example application entry (main.c). The BT app framework has been moved
# to middleware/rt_bt_app and is pulled in via the
# BT_USING_RT_BT_APP Kconfig option.
src = ['main.c']

# Service plugins live under services/ and are gated per-service by their
# BT_USING_XXX macro in services/SConscript.
objs = []
CPPPATH = [cwd, os.path.join(cwd, 'services')]

objs += DefineGroup('Applications', src, depend = ['BT_USING_RT_BT_APP'], CPPPATH = CPPPATH)
objs += SConscript(os.path.join('services', 'SConscript'))

Return('objs')
