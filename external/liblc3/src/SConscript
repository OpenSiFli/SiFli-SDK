from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd, os.path.join(cwd, '..', 'include')]

if not ('ZBT' in BuildOptions and BuildOptions['ZBT'] != 0):
    group = DefineGroup('google_lc3', src, depend = [''], CPPPATH = CPPPATH)

Return('group')